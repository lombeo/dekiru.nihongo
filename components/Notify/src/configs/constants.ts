export enum NotifyDefaultParamsEnum {
    LIMIT = 5,
    OFFSET = 0,
    TYPE = 0,
}

export enum NotifyContentTypeEnum {
    COMMENT = 0,
    VOTE = 1,
    CALL = 2
}

export enum NotifyTypeEnum {
    ALL = 0,
    READ = 1,
    UNREAD = 2
}

export enum NotifyChanelEnum {
    ON_COUNT_NOTIFY_CHANGE = "OnCountNotifyChange",
    RUN_CODE_DONE = "RUN_CODE_DONE"
}

export enum RealtimeCommentChanel {
    COMMENT = 1,
    REALTIME = 14
}