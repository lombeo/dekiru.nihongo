import React from "react";
import { useTranslation } from "next-i18next";

const BoxContact = (props: any) => {
  const { data, status } = props;
  const { t } = useTranslation();

  return <div></div>;
};

export default BoxContact;
