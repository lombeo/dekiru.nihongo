import { ActionIcon as ActionIconMantine } from "@mantine/core";
import React from "react";

const ActionIcon = (props: any) => {
  return <ActionIconMantine {...props} />;
};

export default ActionIcon
